[build]
  builder = "NIXPACKS"

[build.nixpacks]
  install_cmd = "cd backend && npm install && cd ../frontend && npm install && cd ../frontend && npm run build"

[deploy]
  startCommand = "cd backend && npm start"
  restartPolicyType = "ON_FAILURE"
  restartPolicyMaxRetries = 10
